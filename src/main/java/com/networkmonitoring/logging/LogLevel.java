package com.networkmonitoring.logging;

/**
 * Defines the severity levels for log messages.
 */
public enum LogLevel {
    INFO,
    WARNING,
    ERROR,
    DEBUG // Added for more granular debugging if needed
}
